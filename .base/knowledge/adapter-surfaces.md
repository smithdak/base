# Lesson: verify native surfaces before compiling

Captured while implementing the walking skeleton on 2026-07-14. The original spec targeted
repository-local Codex custom prompts, but current Codex loads repository workflows as Agent Skills.
Gate mechanisms also differ within a harness, so fidelity must be reported per gate × target rather
than once per harness.

Promoted to `.base/canon/knowledge/adapter-surfaces.md` and to the default canon emitted by
`base init --global`.

